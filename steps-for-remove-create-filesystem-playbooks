1. # When you run create-filesystem.yml or remove-filesystem.yml run below command
   # these playbook required a variable dev_name value as disk name

Example:
   ansible-playbook create-filesystem.yml -e 'dev_name=sdb'
   ansible-playbook remove-filesystem.yml -e 'dev_name=sdb'
  
